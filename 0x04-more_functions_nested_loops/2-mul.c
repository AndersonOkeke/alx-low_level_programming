#include "main.h"
/**
 * mul - multiplies two integers
 * @a: int to be checked
 * @b: int to be checked
 * Return: the result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
