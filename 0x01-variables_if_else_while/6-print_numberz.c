#include<stdio.h>

/**
 * main - Print Numbers.
 * Return: Always 0 (success)
 */

int main(void)
{

	int a = (char) 0;


	for (a = '0'; a < '10'; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
